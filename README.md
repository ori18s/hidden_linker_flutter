# hidden_linker_flutter

## Getting Started

```shell
flutter run                          # build in debug mode (default)
flutter run -d <device id>
flutter run -d <device id> --release # build in release mode
```

